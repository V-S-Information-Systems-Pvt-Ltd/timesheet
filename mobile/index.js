/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => App);
AppRegistry.registerComponent('vsis-timesheet-mobile', () => App);
AppRegistry.registerComponent('VSIS Timesheet', () => App);
AppRegistry.registerComponent('VsisTimesheetMobile', () => App);
