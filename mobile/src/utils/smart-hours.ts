// Compatibility re-export: the canonical implementation lives in @vsis/core
// and is shared with the web application.
export {
  computeSmartHours,
  timesheetToLogEntry,
  type LogEntry,
} from '@vsis/core';
